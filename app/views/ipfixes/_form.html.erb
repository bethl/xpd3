<%= form_for(@ipfix) do |f| %>
  <% if @ipfix.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ipfix.errors.count, "error") %> prohibited this ipfix from being saved:</h2>

      <ul>
      <% @ipfix.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :host %><br />
    <%= f.text_field :host %>
  </div>
  <div class="field">
    <%= f.label :ip %><br />
    <%= f.text_field :ip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
